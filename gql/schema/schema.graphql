schema {
    query: Query 
    mutation: Mutation
}

type Query {
    user(email: String!): User
}

type Mutation {
    createUser(email: String!, firstName: String!, lastName: String!): User
}

type User {
    id: ID!
    email: String!
    firstName: String!
    lastName: String!
}